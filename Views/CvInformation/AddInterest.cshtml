@model CVBuddy.Models.CVInfo.InterestVM

@if (!ViewData.ModelState.IsValid)
{
    <h4 class="text-danger">You need to make some changes:</h4>
    <ul class="text-danger">
        @foreach (var err in ViewData.ModelState.Values.SelectMany(v => v.Errors))
        {
            <li>
                @err.ErrorMessage
            </li>
        }
    </ul>
}

<h2>Interest</h2>
<form asp-action="AddInterest" method="post">
    <input type="text" asp-for="@Model.InterestName"/>
    <button type="submit" class="d-block btn btn-primary">Create Cv</button>
</form>
