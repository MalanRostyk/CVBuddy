@model CVBuddy.Models.CVInfo.CertificateVM

@if (!ViewData.ModelState.IsValid)
{
    <h4 class="text-danger">You need to make some changes:</h4>
    <ul class="text-danger">
        @foreach (var err in ViewData.ModelState.Values.SelectMany(v => v.Errors))
        {
            <li>
                @err.ErrorMessage
            </li>
        }
    </ul>
}

<h2>Certificate</h2>
<form asp-action="AddCertificate" method="post">

    <input type="text" asp-for="@Model.CertName" />
    <button type="submit" class="d-block btn btn-primary">Create Cv</button>
</form>
