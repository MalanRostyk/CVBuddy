@model CVBuddy.Models.CVInfo.CvVM

@if (!ViewData.ModelState.IsValid)
{
    <h4 class="text-danger">You need to make some changes:</h4>
    <ul class="text-danger">
        @foreach (var err in ViewData.ModelState.Values.SelectMany(v => v.Errors))
        {
            <li>
                @err.ErrorMessage
            </li>
        }
    </ul>
}

<h2>Build your Cv</h2>

<form method="post" asp-action="BuildCv" enctype="multipart/form-data">
    <h3>Upload your image</h3>
    <div>
        <label>Cv Image</label>
        @* <input asp-for="ImageFilePath" type="hidden" value="pic.png"/> *@
        <input asp-for="ImageFile" type="file" accept="image/*" class="form-control img-file-picker" />
    </div>
    <button type="submit" class="d-block btn btn-primary">Create Cv</button>
</form>


<a class="btn btn-primary">Enter Education</a>

<a class="btn btn-primary">Add Skill</a>

<a class="btn btn-primary">Add Experience</a>

<a class="btn btn-primary">Add Certificate</a>

<a class="btn btn-primary">Add Personal Characteristic</a>
@*PersonalCharacteristics*@
<a asp-action="AddInterest" class="btn btn-primary" asp-route-cid="@Model.Cid">Add Interest</a>                                                                                     @*PersonalCharacteristics*@



 